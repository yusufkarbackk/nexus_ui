import { SenderAppForm } from './components/SenderAppForm';

export const metadata = {
  title: 'Create Sender App - Nexus Gateway',
  description: 'Configure a new sender application for data integration',
};

export default function SenderAppsPage() {
  return <SenderAppForm />;
}






